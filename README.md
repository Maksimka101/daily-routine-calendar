# Daily Routine Calendar

Календарь для построения расписания дня с автоматическим расчётом засечек в зависимости от времени сна.

## Структура проекта

```
/src
  /constants
    defaultMarks.js      # Константы дефолтных засечек
  /repositories
    ScheduleRepository.js # Работа с расписаниями в localStorage
    MarkRepository.js     # Работа с засечками в localStorage
  /services
    ScheduleService.js    # Бизнес-логика расписаний
    MarkService.js        # Бизнес-логика засечек
  /utils
    TimeUtils.js          # Утилиты для работы со временем
  index.js                # Точка входа

ARCHITECTURE.md           # Документация архитектуры
example.html              # Пример использования API
```

## Быстрый старт

1. Откройте `example.html` в браузере
2. Используйте кнопки для тестирования API

## Использование

### Создание расписания

```javascript
import { ScheduleService } from './src/index.js';

const scheduleService = new ScheduleService();

// Создать расписание с дефолтными засечками
const schedule = scheduleService.createSchedule(
  'Обычный день',  // название
  '07:00',         // время подъёма
  '22:00'          // время сна
);
```

### Получение засечек

```javascript
import { MarkService } from './src/index.js';

const markService = new MarkService();
const marks = markService.getMarks(schedule.id);

marks.forEach(mark => {
  console.log(`${mark.time} ${mark.emoji} ${mark.title}`);
});
```

### Изменение времени (автосдвиг)

```javascript
// Меняем время подъёма с 07:00 на 09:00
// Утренние засечки автоматически сдвинутся на +2 часа
scheduleService.updateSchedule(schedule.id, {
  wakeTime: '09:00'
});
```

### Добавление пользовательской засечки

```javascript
// Пользовательские засечки НЕ сдвигаются автоматически
markService.createMark(schedule.id, {
  emoji: '📞',
  title: 'Созвон',
  description: 'Важная встреча',
  time: '15:00'
});
```

## Типы засечек

### Дефолтные засечки
Имеют специальные id (`wake`, `breakfast`, `coffee`, и т.д.) и автоматически сдвигаются при изменении времени:

- **Утренние** (сдвигаются при изменении `wakeTime`):
  - `wake` - Подъём
  - `breakfast` - Завтрак
  - `coffee` - Последний кофе
  - `lunch` - Обед

- **Вечерние** (сдвигаются при изменении `bedtime`):
  - `gym` - Спортзал
  - `dinner` - Ужин
  - `no-screens` - Без экранов
  - `sleep` - Сон

### Пользовательские засечки
Имеют UUID и **НЕ сдвигаются** автоматически.

## API

Подробная документация API находится в `ARCHITECTURE.md`.

### ScheduleService

- `getSchedules()` - получить все расписания
- `createSchedule(name, wakeTime, bedtime)` - создать расписание
- `updateSchedule(id, data)` - обновить расписание
- `deleteSchedule(id)` - удалить расписание

### MarkService

- `getMarks(scheduleId)` - получить засечки расписания
- `createMark(scheduleId, data)` - создать засечку
- `updateMark(id, data)` - обновить засечку
- `deleteMark(id)` - удалить засечку

### TimeUtils

- `parseTime(time)` - '07:30' → 450 минут
- `formatTime(minutes)` - 450 → '07:30'
- `shiftTime(time, delta)` - сдвинуть время
- `getTimeDelta(old, new)` - разница между временами

## Примеры

См. `example.html` для интерактивных примеров.
